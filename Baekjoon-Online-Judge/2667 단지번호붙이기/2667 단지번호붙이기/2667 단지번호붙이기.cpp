
#include<stdio.h>
#include<vector>

vector<int> cnt;

int g(int n. int m)

int main(void) {
	int N;	scanf("%d", &N);
	for (int i = 1 ; i <= N ; i++) {
		for (int j = 1; j <= N; j++)
			scanf("%d", &arr[i][j]);
	}
	for (int i = 1; i <= N; i++) {
		for (int j = 1; j <= N; j++)
			scanf("%d", &arr[i][j]);
	}

}
